<UserControl
    x:Class="Labb2Databaser.Views.StartView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Labb2Databaser.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Button
                Grid.Row="0"
                Margin="250,40,250,20"
                Background="RosyBrown"
                Command="{Binding NavigateAuthorsView}"
                Content="Authors and Books"
                FontSize="18" />
            <Button
                Grid.Row="1"
                Margin="250,20,250,40"
                Background="RosyBrown"
                Command="{Binding NavigateInventoryBalanceView}"
                Content="InventoryBalance"
                FontSize="18" />
        </Grid>
    </Grid>
</UserControl>
